version: '3'

services:
  frontend:
    image: node:latest
    container_name: vue_app
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "3005:3000"
    command: sh -c "npm install && npm run dev"
